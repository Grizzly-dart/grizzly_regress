name: grizzly_regress
description: Regression models for Dart
version: 0.0.2-dev
homepage: https://github.com/Grizzly-dart/grizzly_regress
author: Ravi Teja Gudapati <tejainece@gmail.com>
documentation:

environment:
  sdk: '>=1.20.1 <2.0.0'

dependencies:
  meta: ^1.1.2
  grizzly_array:
    path:
      ../grizzly_array
  grizzly_linalg:
    path:
      ../grizzly_linalg

dev_dependencies:
  test:

dependency_overrides:
  grizzly_array:
    path:
      ../grizzly_array
  grizzly_linalg:
    path:
      ../grizzly_linalg
  grizzly_primitives:
    path:
      ../grizzly_primitives